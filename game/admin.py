"""
Django admin configuration for the game app.

Since this game doesn't use any database models,
no admin configuration is needed.
"""

from django.contrib import admin

# No models to register since we're using session-based storage
